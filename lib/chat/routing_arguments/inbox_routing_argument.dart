class InboxRoutingArgument {
  String? name, image;
  int? id;
  bool? notificationNavigationEnable;

  InboxRoutingArgument(
      {this.name, this.image, this.id, this.notificationNavigationEnable});
}
